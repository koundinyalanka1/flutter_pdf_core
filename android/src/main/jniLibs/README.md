# Prebuilt native libraries

Run `scripts/build_android.sh` from the package root to populate this
directory with `libpdf_ffi.so` for each ABI (arm64-v8a, armeabi-v7a,
x86_64). The Android library project bundles everything in here
automatically.
